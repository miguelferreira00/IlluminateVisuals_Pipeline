[build]
builder = "nixpacks"
buildCommand = "cd backend && mvn clean package -DskipTests -q"
startCommand = "java -jar backend/target/crm-0.0.1-SNAPSHOT.jar"

[deploy]
startCommand = "java -jar backend/target/crm-0.0.1-SNAPSHOT.jar"
port = 8080
